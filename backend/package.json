 {
   "name": "restaurant-finder-backend",
  "version": "2.0.0",
   "description": "Backend for the Restaurant Finder application",
   "main": "index.js",
   "scripts": {
@@ -13,6 +15,8 @@
     "test": "echo \"Error: no test specified\" && exit 1"
   },
   "dependencies": {
    "@googlemaps/markerclusterer": "^2.0.0",
    "dotenv": "^10.0.0",
     "express": "^4.17.1",
     "mongoose": "^5.12.3",
     "bcryptjs": "^2.4.3"
   },
   "devDependencies": {
    "nodemon": "^2.0.7"
   }
 }

diff --git a/frontend/package.json b/frontend/package.json
index 1234567..89abcde 100644
++ b/frontend/package.json
@@ -1,5 +1,7 @@
 {
   "name": "restaurant-finder-frontend",
  "version": "2.0.0",
   "description": "Frontend for the Restaurant Finder application",
   "main": "index.js",
   "scripts": {
@@ -13,6 +15,8 @@
     "test": "echo \"Error: no test specified\" && exit 1"
   },
   "dependencies": {
    "@react-google-maps/api": "^2.0.0",
    "dotenv": "^10.0.0",
     "react": "^17.0.2",
     "react-dom": "^17.0.2",
     "react-scripts": "4.0.3"
   },
   "devDependencies": {
    "nodemon": "^2.0.7"
   }
 }
