   "dependencies": {
     "react": "^17.0.2",
     "react-dom": "^17.0.2",
    "@react-google-maps/api": "^2.9.0", // Google Maps integration
    "styled-components": "^5.3.3" // For dark mode styling
   },
   "devDependencies": {
     "@babel/core": "^7.14.6",
@@ -28,6 +36,10 @@
     "react-scripts": "4.0.3"
   }
 }

// Add scripts for building and running in dark mode
"scripts": {
  "start:dark": "REACT_APP_THEME=dark react-scripts start",
  "build:dark": "REACT_APP_THEME=dark react-scripts build"
}
